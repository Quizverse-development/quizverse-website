import { WorldFlagsQuiz } from "./world-flags";
import { Quiz } from "../game-store";

// Export only the World Flags quiz
export const ENHANCED_QUIZZES: Quiz[] = [
  WorldFlagsQuiz,
  // Other quizzes removed as requested
];